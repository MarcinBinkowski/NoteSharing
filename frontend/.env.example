# Vite frontend environment variables
# Copy this file to .env and adjust values for local development.
# IMPORTANT: Only variables prefixed with VITE_ are exposed to the browser bundle.

# API base URL — used by the Vite dev-server proxy to forward /api requests.
# Leave empty when running via Docker Compose (proxy auto-configured).
VITE_API_URL=http://localhost:8000
