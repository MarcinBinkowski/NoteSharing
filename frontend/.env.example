VITE_API_URL=http://localhost:8000

# API path prefix — used to construct absolute links like the Google OAuth redirect.
# Set to the full origin + prefix when the API lives on a different domain.
VITE_API_BASE=/api
